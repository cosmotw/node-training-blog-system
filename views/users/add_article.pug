extends ../layout

block content
  h1 Add Article
  form(name="add_article", action="/apis/add", method="post")
    #content
      textarea(name="content", cols="100", rows="25")
    #button-group
      input(type="submit", value="Submit")